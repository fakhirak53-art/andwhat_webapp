Keep code simple, beginner friendly, readable, and maintainable.
Do not over-engineer or add unnecessary abstractions.
Keep files reasonably small and split large pages/components into logical sections.
Reuse components where it actually improves consistency and maintenance.
Do not create reusable abstractions for one-time use unless it clearly helps readability.
Preserve existing functionality when updating a project.
Follow the project’s framework and existing conventions unless they are clearly harmful.
If Tailwind is available, prefer simple Tailwind utility classes and avoid unnecessary custom CSS.
Keep styling consistent and avoid complex class strings or mixed styling approaches without reason.
Centralize theme values like colors, spacing, and design tokens where practical.
Remove unused imports, dead code, duplicate code, unnecessary wrappers, and debug code.
Improve accessibility using semantic HTML, labels, alt text, and logical heading structure.
Improve SEO using proper titles, descriptions, social metadata, canonical URLs, semantic structure, and relevant structured data where appropriate.
Improve performance using framework best practices, optimized images, and minimal unnecessary client-side code.
Do not add unnecessary libraries.
Do not rewrite working code for no practical benefit.
If assets are missing, continue with safe placeholders and document them clearly in docs/image-requirements.md.
Final output should include summary, file changes, docs created/updated, and remaining asset/content needs.
